# Production build config for VESTA.
#
# Committed deliberately, and none of it is a secret: the API base is a public
# address by design and the app holds no key. Her PIN is typed in, never built
# in. Nothing secret may ever be added to this file — the repo is PUBLIC.

# ⚠ The Pi's Tailscale FUNNEL address — genuinely reachable from the open
# internet, which is the whole point: she has no Tailscale and no NEURO PIN.
# `tailscale funnel status` confirms this host proxies / → 127.0.0.1:3001.
VITE_API_URL=https://pi5.tailecb90f.ts.net
